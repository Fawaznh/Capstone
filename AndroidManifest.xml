<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="capstone.wumaps" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <!--
 The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but are recommended.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="WUMaps"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="WUMaps" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FindClassActivity"
            android:label="FindClassActivity" >
        </activity>
        <activity
            android:name=".MapsActivity"
            android:label="MapsActivity" >
        </activity>
        <activity
            android:name=".EventsActivity"
            android:label="EventsActivity" >
        </activity>

        <activity
            android:name=".MyCoursesActivity"
            android:label="My Courses" >
        </activity>

        <activity
            android:name=".AddCourseActivity"
            android:label="Add New Class" >
        </activity>

        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value= "@string/google_maps_key" />
           <!-- android:value="AIzaSyAmrVwO40t70TjVSKj51Z3VnNt71IDpGdw" /> -->


    </application>

</manifest>